<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="ir_cron_mollie_capture_transaction" model="ir.cron">
        <field name="name">Mollie: Capture Transaction</field>
        <field name="model_id" ref="model_payment_transaction"/>
        <field name="interval_number">1</field>
        <field name="interval_type">hours</field>
        <field name="state">code</field>
        <field name="code">model._cron_mollie_capture_transaction()</field>
    </record>

</odoo>
